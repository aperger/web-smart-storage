import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend/smart-storage-frontend',
  webDir: 'www'
};

export default config;
